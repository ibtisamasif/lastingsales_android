<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Specify a semi-transparent solid green background color -->
    <solid android:color="#233691"

        />


    <corners
        android:topLeftRadius="0dp"
        android:topRightRadius="@dimen/corners_radius_size"
        android:bottomRightRadius="@dimen/corners_radius_size"
        android:bottomLeftRadius="0dp" />

    <!-- Specify the margins that all content inside the drawable must adhere to -->
<stroke android:color="#233691" android:width="2dp"/>

</shape>