<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Specify a semi-transparent solid green background color -->
    <solid android:color="#ffffff"

        />


    <corners
        android:topLeftRadius="@dimen/corners_radius_size"
        android:topRightRadius="0dp"
        android:bottomRightRadius="0dp"
        android:bottomLeftRadius="@dimen/corners_radius_size" />

    <!-- Specify the margins that all content inside the drawable must adhere to -->
<stroke android:color="#0071BC" android:width="2dp"/>

</shape>