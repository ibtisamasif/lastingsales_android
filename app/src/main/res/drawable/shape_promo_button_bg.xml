<?xml version="1.0" encoding="utf-8"?>
<shape
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <!-- Specify a semi-transparent solid green background color -->
    <solid android:color="#ff7400"
        />


    <!-- Specify a dark green border -->
    <stroke
        android:width="2dp"
        android:color="#ff6300"
        />
    <corners android:radius="@dimen/corners_radius_size_promo_button" />
    <!-- Specify the margins that all content inside the drawable must adhere to -->
    <padding
        android:left="2dp"
        android:right="2dp"
        android:top="2dp"
        android:bottom="2dp"
        />
</shape>